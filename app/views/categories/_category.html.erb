<%# Static category fragment - keyed on the record's id + updated_at. %>
<% cache [ "categories/category", category ] do %>
<div id="<%= dom_id category %>">
  <p class="my-5">
    <strong class="block font-medium mb-1">Name:</strong>
    <%= category.name %>
  </p>
</div>
<% end %>
