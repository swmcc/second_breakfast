# Production destination -- deployed with `bin/kamal deploy -d production`.
# Merged on top of config/deploy.yml.
#
# TODO(owner): replace the placeholder host and domain below with the real
# production server before the first deploy. Everything else is inherited.

service: second_breakfast

servers:
  web:
    - 192.168.0.1

proxy:
  ssl: true
  host: app.example.com

env:
  clear:
    SOLID_QUEUE_IN_PUMA: true
